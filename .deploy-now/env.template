# IONOS MariaDB – fill via GitHub Secrets (Settings → Secrets and variables → Actions).
# Add secrets: DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME
# This file is used for PHP project deployment; values are replaced at deploy time.
DB_HOST=$DB_HOST
DB_PORT=$DB_PORT
DB_USER=$DB_USER
DB_PASSWORD=$DB_PASSWORD
DB_NAME=$DB_NAME
